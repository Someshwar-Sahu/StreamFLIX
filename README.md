# StreamFlix (Learning Project)

Netflix-style streaming platform — Web, Mobile, Desktop — built solo as a learning/portfolio project.
No purchased/licensed content is used, so DRM is intentionally out of scope.

**Read `docs/AI_COLLABORATION_RULES.md` FIRST — before writing or suggesting any code.**
Then `docs/PROJECT_STATUS.md` for current state.